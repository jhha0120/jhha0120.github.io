if ( document.querySelector('.artwork-wrapper') !== null ) {
  const subNav = document.querySelector('.nav-sub');
  subNav.style.display = 'flex';
};

// const menuItem = document.querySelectorAll('.nav-main > li');
// menuItem.forEach((i) => { i.addEventListener('mouseover', (e) => { console.log(e)}) });
